name: Test Workflow

defaults:
  run:
    shell: bash --noprofile --norc -euo pipefail {0}

on:
  workflow_call:
  workflow_dispatch:

jobs:
  say-hi:
    runs-on: ubuntu-latest
    steps:
      - run: echo Hi from level 1
  call-reusable-workflow:
    uses: ./.github/workflows/level-2.yml
